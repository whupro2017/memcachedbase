//
// Created by iclab on 1/7/20.
//

#include <memcached.h>

int main(int argc, char **argv) {
    conn con;
    /*item_get("dummy", 5, &con, false);*/
    /*conn *pcon = conn_new(0, conn_listening, EV_READ | EV_PERSIST, 1, (network_transport) 0, NULL, NULL);
    conn_close_idle(pcon);*/
    return 0;
}